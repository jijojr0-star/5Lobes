# Dependencies
node_modules/

# Production build
build/

# Environment files — never commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# .env.production is safe to commit (no secrets if using Vercel env vars dashboard)
# If you keep secrets in .env.production, add it here too:
# .env.production

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.DS_Store
.vscode/

.vercel
